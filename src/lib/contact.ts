/** Public sales inbox — shown on site and used as form destination. */
export const SALES_EMAIL = 'sales@kaferlabs.com'

/** FormSubmit AJAX endpoint (static hosting friendly, no backend). */
export const CONTACT_FORM_ENDPOINT = `https://formsubmit.co/ajax/${SALES_EMAIL}`
